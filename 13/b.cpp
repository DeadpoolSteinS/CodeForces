#include <bits/stdc++.h>
#define int long long
#define endl '\n'
#define sync ios::sync_with_stdio(0);
#define fast cin.tie(0)
#define pii pair<int, int>
using namespace std;

signed main()
{
    sync fast;
    int t;
    cin >> t;
    while (t--)
    {
        int n, m;
        cin >> n >> m;
        string area[n];
        for (int i = 0; i < n; i++)
        {
            cin >> area[i];
        }
    }
    return 0;
}

// 1 2 3 4
// 1 3 2 4
// 2 3 1 4
// 3 4 1 2